[defaults]
inventory = inventory/hosts.yml
remote_user = silas
# Lab nodes are rebuilt often, so host keys churn constantly
host_key_checking = False
interpreter_python = auto_silent

[ssh_connection]
pipelining = True
